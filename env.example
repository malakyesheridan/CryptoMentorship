# Production Database (Neon PostgreSQL)
# Get your connection string from Neon console: https://console.neon.tech
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"
DIRECT_URL="postgresql://user:password@host:port/database?sslmode=require"

# Development Database (SQLite - for local development)
DATABASE_URL_DEV="file:./dev.db"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:5001"
NEXTAUTH_SECRET="your_generated_secret_here"

# Google OAuth (optional - enables Google sign-in)
# Get these from https://console.developers.google.com/
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Email Configuration (optional - enables email sign-in and notifications)
# Example using ProtonMail with custom domain:
# EMAIL_SERVER="smtp://coen@stewartandco.org:SMTP_TOKEN@smtp.protonmail.ch:587"
# EMAIL_FROM="Coen <coen@stewartandco.org>"
# 
# For ProtonMail:
# 1. Generate SMTP token: Settings → Proton Mail → IMAP/SMTP → SMTP tokens
# 2. Use smtp.protonmail.ch:587 with STARTTLS
# 3. Username: your full email address (e.g., coen@stewartandco.org)
# 4. Password: the generated SMTP token (not your account password)
#
# Example using Mailtrap for development:
# EMAIL_SERVER="smtp://username:password@smtp.mailtrap.io:2525"
# EMAIL_FROM="Crypto Portal <no-reply@example.com>"

# Public environment variables (for client-side provider detection)
NEXT_PUBLIC_GOOGLE_ENABLED="false"
NEXT_PUBLIC_EMAIL_ENABLED="false"

# Port fallback preferences (comma-separated)
PORT_PREFERRED="5000,5001,3000"

# Optional: Supabase (if you want real-time chat)
# SUPABASE_URL=""
# SUPABASE_ANON_KEY=""

# Vercel Blob Storage (required for uploads)
# Get from: https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=""

# Stripe Configuration (optional - for payment processing)
# Get these from https://dashboard.stripe.com/
STRIPE_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Stripe Price IDs (create products/prices in Stripe Dashboard)
# Example: https://dashboard.stripe.com/products -> Create Product -> Copy Price ID
# Foundation (T1)
STRIPE_PRICE_T1_MONTHLY="price_..."
STRIPE_PRICE_T1_3MONTH="price_..."
STRIPE_PRICE_T1_6MONTH="price_..."
STRIPE_PRICE_T1_ANNUAL="price_..."
# Growth (T2)
STRIPE_PRICE_T2_MONTHLY="price_..."
STRIPE_PRICE_T2_3MONTH="price_..."
STRIPE_PRICE_T2_6MONTH="price_..."
STRIPE_PRICE_T2_ANNUAL="price_..."
# Elite (T3)
STRIPE_PRICE_T3_MONTHLY="price_..."
STRIPE_PRICE_T3_3MONTH="price_..."
STRIPE_PRICE_T3_6MONTH="price_..."
STRIPE_PRICE_T3_ANNUAL="price_..."

# Referral System Configuration (optional)
REFERRAL_SYSTEM_ENABLED="true"  # Set to "false" to disable referral system
REFERRAL_COMMISSION_RATE="0.15"  # 15% commission rate (0.15 = 15%)
REFERRAL_COOKIE_EXPIRY_DAYS="30"  # Days to keep referral cookie
REFERRAL_CODE_EXPIRY_DAYS=""  # Optional: Days until referral code expires (leave empty for no expiration)
REFERRAL_HOLD_DAYS="30"  # Days after qualification before payout is payable
NEXT_PUBLIC_APP_URL="https://yourdomain.com"  # Base URL for affiliate links
