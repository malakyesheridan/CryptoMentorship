{
  "buildCommand": "next build",
  "comment": "⚠️ DO NOT store credentials here - set all environment variables in Vercel Dashboard",
  "env": {
    "DATABASE_URL": "⚠️ Set in Vercel Dashboard",
    "DIRECT_URL": "⚠️ Set in Vercel Dashboard",
    "NEXTAUTH_SECRET": "⚠️ Set in Vercel Dashboard (use: npm run generate-secret)",
    "NEXTAUTH_URL": "⚠️ Set in Vercel Dashboard (your production URL)"
  }
}
